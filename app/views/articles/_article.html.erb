<div class="container">

<% @articles.each do |article| %>
	<div class="row justify-content-md-center">
		<div class="col-8 mt-4">
			<div class="card text-center shadow mb-5 bg-white rounded">
			  <div class="card-header font-italic font-weight-bold">
			    by: <%= link_to article.user.username, user_path(article.user) %>
			  </div>
			  <div class="card-body">
			    <h5 class="card-title"><%= link_to article.title, article_path(article), class: "text-success font-weight-bold" %></h5>
			    <p class="card-text"><%= truncate(article.description, length: 100) %></p>
			    <%= link_to 'View', article_path(article), class: "btn btn-success"%>
			    <%= link_to 'Edit', edit_article_path(article), class: "btn btn-primary"%></a>
			    <%= link_to 'Delete', article_path(article),class: "btn btn-danger", method: :delete, data: { confirm: "Are you sure?", }%></a>
			  </div>
			  <div class="card-footer text-muted">
			    Created: <%= time_ago_in_words(article.created_at) %> | Edited: <%= time_ago_in_words(article.updated_at) %>
			  </div>
			</div>
		</div>
	</div>
<% end %>
</div>
